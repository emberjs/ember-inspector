<h1>
  {{#if isNested}}
    <button {{action popStack}}>◀ Back</button>
  {{else}}
    <span>◀ Back</span>
  {{/if}}
  <span data-label="object-name">{{firstObject.name}}</span>
</h1>
<h2>{{trail}}</h2>
{{render "mixinDetails"}}
