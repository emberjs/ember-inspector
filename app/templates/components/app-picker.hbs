<div class="dropdown">
  <select
    class="dropdown__select"
    onchange={{action "selectApp" value="target.value"}}
  >
    {{#each apps as |app|}}
      <option
        value={{app}}
        selected={{eq app selectedApp}}
      >
        {{app}}
      </option>
    {{/each}}
  </select>
  {{svg-jar "dropdown-arrow" class="dropdown__arrow"}}
</div>
