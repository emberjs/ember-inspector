{{#resizable-column
  class=@classes
  width=@width
}}
  {{yield}}
{{/resizable-column}}

<DragHandle
  @minWidth={{this.minWidth}}
  @on-drag={{this.triggerResize}}
  @position={{@width}}
  @setIsDragging={{@setIsDragging}}
  @side={{@side}}
/>
