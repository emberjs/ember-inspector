<ResizableColumn
  class={{@classes}}
  @width={{@width}}
>
  {{yield}}
</ResizableColumn>

<DragHandle
  @minWidth={{this.minWidth}}
  @on-drag={{this.triggerResize}}
  @position={{@width}}
  @setIsDragging={{@setIsDragging}}
  @side={{@side}}
/>
