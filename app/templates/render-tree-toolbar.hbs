<div class="toolbar">
  {{clear-button action="clearProfiles" classNames="toolbar__icon-button"}}
  <div class="toolbar__search js-render-profiles-search">
    {{input value=searchField placeholder="Search"}}
    {{#if searchField}}
      <button {{action "clearSearchField"}} class="toolbar__icon-button toolbar__search-clear-button" title="clear">
        {{icon-input-clear}}
      </button>
    {{/if}}
  </div>
  <div class="filter-bar__pills"></div>

  {{#unless showEmpty}}
    <div class="divider"></div>
    <button class="js-toolbar-page-refresh-btn" {{action "refreshPage"}}>Reload</button>
  {{/unless}}

</div>
