<td
  class="list__cell
    {{if this.clickable 'list__cell_clickable'}}
    {{if this.highlight 'list__cell_highlight'}}"
  role="button"
  style={{this.safeStyle}}
  title={{this.title}}
  {{on "click" this.click}}
  ...attributes
>
  {{yield}}
</td>