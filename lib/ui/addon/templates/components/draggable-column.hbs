<Ui::ResizableColumn
  class={{@classes}}
  @width={{@width}}
>
  {{yield}}
</Ui::ResizableColumn>

<Ui::DragHandle
  @minWidth={{this.minWidth}}
  @on-drag={{this.triggerResize}}
  @position={{@width}}
  @side={{@side}}
/>
